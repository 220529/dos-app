FIRST   DB 'ASDAASZXCFGRSAAXADRRAT… $'
NUMBER  DB ?

DATA ENDS

CODE SEGMENT
    ASSUME CS:CODE, DS:DATA

START:
    MOV AX, DATA
    MOV DS, AX
    LEA DI, FIRST
    MOV BL, 0

NEXT:
    MOV AL, [DI]
    INC DI
    CMP AL, '$'
    JE  EXIT
    CMP AL, 'A'
    JNE NEXT
    INC BL
    JMP NEXT

EXIT:
    MOV NUMBER, BL
    MOV AH, 4CH
    INT 21H

CODE ENDS
END START